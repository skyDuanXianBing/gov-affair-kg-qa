# 政务大模型：论文启示与改进路线

> 基于《论文/》目录下 7 篇 KG 论文（中文翻译）与本仓库现状调研，梳理出的改进建议。
> 调研与论文分析完成于 2026-08-27。

---

## 一、论文结论速览

| 论文 | 一句话结论 | 对本项目的作用点 |
|---|---|---|
| AutoGraph-R1 | 用强化学习让"建图"直接为"问答"服务，奖励用"答案可从子图推导(是/否)"或"黄金段落召回率"，而非最终答案 F1 | 远期闭环：以多跳问答效果反哺建图质量 |
| DuetGraph | 检索/推理用"局部+全局双通路融合"和"粗排→精排、允许低分候选翻盘"，缓解分数过度平滑 | 检索层：两段式检索 + 冷门候选保留 |
| SocraticKG | 抽三元组前先用 5W1H 生成"上下文独立"的 QA 对中间层，事实保留率最高 96.3%，图更连通 | 建图层：受理条件等隐式逻辑的抽取方法 |
| Wikontic | 从 Wikidata 派生本体约束库（2464 属性 + 域/值域约束）+ 实体规范化 + 别名表，仅 3.5% 三元组错位 | 建图层：schema 约束校验 + 实体归一 |
| Query-Aware (QSRAG) | 检索打分显式建模"问题语义 × 关系类型"交互；三元组带置信度喂 LLM；单次检索+单次生成 | 检索层：多跳 locate 打分与证据组织 |
| HotpotQA | 沿文档引用边生成多跳问答对 + 句子级支撑事实；"给定材料/全库检索"两档评测 + 答案依据联合 F1 | 评测层：QA 对构造方法与评测协议 |
| SynthKG (Distill) | 分块 → 去语境化 → 逐块抽"命题+三元组"，用合成数据蒸馏 8B 模型，成本降约 97% | 建图层：长文本结构化抽取的完整流水线 |

---

## 二、项目现状（关键事实）

- **可跑**：清洗管线（157 万条 JSONL）→ 纯规则映射建图（无 LLM 抽取，试点 1 万条 100% 对账）→ bge-m3 向量化 → `qa/` 单轮检索 + KAG 式多跳 + WebUI。
- **大缺口 1：建图侧无 LLM 抽取**。acceptCondition/windowProcess/onlineProcess 等长文本整块存为属性，检索时截断（`qa/retriever.py:46-47`），图谱里最值值的知识未被结构化。
- **大缺口 2：两套 schema 割裂**。新分类 schema（`schema/gov_service.schema`，namespace ZwdmxGJ，15 实体 + Chunk + KnowledgeModel）代码完整但未灌图；`qa/multihop.py` 白名单仍只认旧 `GovAffair.*` 的 8 种关系。
- **大缺口 3：无评测体系**。无 QA 对、无指标、无回归脚本。`kg/pilot/qa_probe/` 已暴露缺陷：检索空手时 LLM 泛泛作答、引用为空。
- 其他：无实体归一（LegalBasis 仅 1219 节点扛 13.5 万引用，热点塌缩；同名多区县事项靠检索端 name 去重补救）；检索纯余弦硬阈值（0.45/0.72/0.8），无重排、无问题-关系交互打分。

---

## 三、论文 → 缺口 对照

| 论文 | 对应缺口 |
|---|---|
| SynthKG | 长文本抽取成三元组的完整流水线 + 命题中间层（可溯源） |
| SocraticKG | 受理条件/办理流程这类隐式规则的抽取方法 |
| Wikontic | 新 schema 的约束校验 + 实体归一（LegalBasis 热点问题） |
| QSRAG | 多跳 locate 环节"问题-关系匹配"打分 |
| DuetGraph | 检索两段式（粗排→精排）+ 冷门候选翻盘 |
| HotpotQA | 多跳评测集怎么造（沿图边生成 QA 对 + 支撑条款） |
| AutoGraph-R1 | 后期用"答案可推导性"做奖励闭环优化建图 |

---

## 四、行动路线（按优先级）

### 第 1 步：统一到新 schema，打通 qa/（最紧急，不依赖论文）

先灌新 schema 图，再把 `qa/multihop.py` 关系白名单从旧 8 种扩到新 18 种，让 `KnowledgeModel.retrievalFilter` 真正被检索端消费。此步不做，后续改进都落在旧图上白费。

### 第 2 步：长文本属性 → 结构化知识（SynthKG + SocraticKG，收益最大）

1. **分块**：复用 `scripts/chunk_openspg_documents.py`（2000/200 字符句界+重叠）；论文推荐更小的块（约 256 token），可两档对比密度。
2. **去语境化**：把"本机关、该事项、申请人"等指代改写为规范全称（政务公文指代严重，此步不可省）。
3. **抽取"命题 + 三元组"**：命题保留金额、时限、条件原文，兼当抽取思维链与检索证据单元；三元组挂到 GovernmentService。
4. 复杂受理条件用 SocraticKG 的追问式（什么人/什么条件/怎么办）prompt 展开，把"如果是…则需要…"抽成 ServiceCondition 边。

成本路线（SynthKG）：先用 DeepSeek 跑试点，量大后微调本地 8B 模型（论文证明蒸馏后可打平 70B，成本约 3%，且数据不出网）。

### 第 3 步：实体归一 + schema 约束校验（Wikontic 照搬）

- 每类实体建**别名表 + 向量索引**（"市发改委"→"发展和改革委员会"），导入时规范化合并；
- 为新 schema 18 种关系定义**域-值域约束**，进图前校验（激活已有的 validate 脚本），不合格由 LLM 重建；
- 用**本体错位率**（论文仅 3.5%）做建图质量监控；错位三元组不删，进人工审核队列。

### 第 4 步：检索层升级（QSRAG + DuetGraph，改动小见效快）

- 多跳 locate 加**"问题-关系"打分**：候选打分引入问题向量 × 关系/实体类型交互；最简版是让 LLM 先选关系类型再遍历（治"问审批部门抓到协办部门"）；
- 三元组**带置信度**喂生成器（QSRAG 实测稳定提分）；
- **粗排→精排**（DuetGraph）：先向量粗排扩大候选，再精排精选，给低分候选保留名额；
- 修复 `qa_probe` 暴露的缺陷：检索为空时显式拒答并返回"检索到 0 条证据"，禁止 LLM 泛泛作答（产品+合规双重问题）。

### 第 5 步：建评测体系（HotpotQA 方法，最缺也最便宜）

1. 沿图边自动生成多跳 QA 对：GovernmentService 出发走 2-3 跳（事项→部门→上级、事项→材料→共用材料的其他事项），LLM 生成问题 + **支撑节点/条款作为标准依据**；人工抽检 50~100 条。
2. 指标：EM/F1 + 答案与依据的**联合 F1**（依据错即半错——政务场景依据错了就是事故）。
3. 两档测试：给定候选材料（考推理）/ 全库检索（考检索）。
4. 做成回归脚本，每次改检索或建图参数跑一遍。

### 第 6 步（远期）：AutoGraph-R1 闭环

评测体系产出"问题-答案-支撑节点"数据后即具备训练素材。先用省钱捷径 RAFT（用"答案能否从子图推导出 Yes/No"过滤抽取结果再微调），让建图质量围着多跳问答效果转，而非三元组数量。

---

## 五、优先级总结

```
紧急且便宜：① 统一新 schema 打通 qa/ → ⑤ 建评测集（互相成就，先做）
收益最大：  ② 长文本命题化抽取（SynthKG+SocraticKG）
数据质量：  ③ 实体归一 + 约束校验（Wikontic）
检索快赢：  ④ 问题-关系打分 + 置信度 + 空检索拒答（QSRAG）
远期闭环：  ⑥ RAFT/RL 优化建图（AutoGraph-R1，依赖⑤）
```

**第一个里程碑建议**：新 schema 灌图 + 50 条多跳评测集跑出基线数字。有了基线，后续每一步改进才有说服力，也是论文/答辩最硬的证据链。
