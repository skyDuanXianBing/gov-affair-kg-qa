# 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会入库）
# DeepSeek API key：自建 RAG（qa/generator.py）自动加载项目根 .env，
# 优先级：shell 环境变量 DEEPSEEK_API_KEY > .env > kag_config.yaml（仓库内为占位符）。
# KAG 原生工具（knext project update 等）不读环境变量，需临时在
# kg/pilot/GovAffair/kag_config.yaml 填入真实 key（本地改动勿提交）。
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 内网 OpenAI 兼容 LLM（scripts/extract_propositions_pilot.py 命题抽取试点）：
# EXTRACT_BASE_URL  Chat Completions 根地址，服务端在拼接时追加 /chat/completions
#                   （默认 http://10.130.71.10:30799/v1，无需 key）
# EXTRACT_MODEL     模型名（默认 qwen3.8-27b，思考型：只读取 message.content，
#                   忽略 message.reasoning_content）
# EXTRACT_API_KEY   可选；设置后在请求头追加 Authorization: Bearer <key>
#EXTRACT_BASE_URL=http://10.130.71.10:30799/v1
#EXTRACT_MODEL=qwen3.8-27b
#EXTRACT_API_KEY=
